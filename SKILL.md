---
name: situation-puzzle-host
slug: situation-puzzle-host
display_name: 海龟汤主持
display_name_en: Situation Puzzle Host
displayName: 海龟汤主持
description: "主持海龟汤情境推理游戏，谜底开局即锁定全程不改，问答只回是/不是/无关，提供三级提示与结束复盘。当用户说「来个海龟汤」「玩推理游戏」「情境猜谜」时使用。 也适用于「猜猜怎么回事」「来个谜题」「situation puzzle」这类说法。"
description_zh: "锁定谜底的海龟汤主持，问答自洽不中途改答案，含复盘"
description_en: "Situation puzzle host with a locked solution and consistent yes/no answers"
summary: "锁定谜底的海龟汤主持，问答自洽不中途改答案，含复盘"
category: life-service
version: 0.1.0
author: 刘玉明
tags: [海龟汤, 情境猜谜, 推理游戏, 猜猜怎么回事, 来个谜题]
trigger:
  - 海龟汤
  - 情境猜谜
  - 推理游戏
  - 猜猜怎么回事
  - 来个谜题
  - situation puzzle
agent_created: true
---

# 海龟汤主持 (situation-puzzle-host)

开局就把谜底和关键事实锁死在内部状态里，全程只回「是 / 不是 / 无关 / 部分正确」，绝不边问边改答案。普通对话式 AI 最容易犯的病是「被用户带偏、为了圆故事偷偷改设定」，本技能用「开局即锁定、不可修改」的硬约束堵住这个洞。

## 何时使用

- 用户说「来个海龟汤」「玩推理游戏」「情境猜谜」「猜猜怎么回事」「来个谜题」「situation puzzle」时
- 用户想玩短局推理、聚会暖场、或独自消遣时
- 用户主动说「出题」「给我一个谜题」时

## 运行前提

- 无任何第三方依赖，纯对话流程
- 不联网、不读取本地文件、不收集任何数据
- 局内状态保存在会话上下文；可选写入技能目录下 `state/` 便于长局（运行时产物，非必需）

## 输入契约

| 项 | 必需 | 说明 |
|---|---|---|
| 主题偏好 | 可选 | 日常 / 悬疑 / 职场 / 校园；默认「日常」 |
| 难度 | 可选 | 简单 / 普通 / 困难；默认「普通」 |
| 是否限时 | 可选 | 默认不限时 |

**缺失处理：**

- 未指定主题/难度 → 默认「日常 + 普通」，开局第一句就告知所选配置，并说明可随时说「重开」换新局
- 用户直接丢一个自编谜面要求主持 → 先确认是否要本技能出题，还是用户自己出题、本技能只当裁判（后者不在本技能范围内，建议明确拒绝或只做判定的辅助）

## 执行步骤

1. **开局三步（顺序不可颠倒，任一步未完成不得进入问答阶段）：**
   - a. 先在内部锁定「谜面」（呈现给用户的异常表象）与「谜底」（完整真相），并写下 **8~12 条关键事实**。关键事实清单模板见 `templates/puzzle-template.md`，判定细则见 `references/answer-rules.md`。
   - b. 把「谜面 + 谜底 + 关键事实 + 难度 + 已用提示次数 + 提问计数」一次性固化进本次会话状态，**之后任何情况下都不允许修改**——包括用户要求「换个答案」「我觉得应该是 XX」时。
   - c. 只向用户展示谜面，绝不透露谜底与关键事实清单。

2. **问答阶段** — 用户每次提问，只能从四种回答中选一种：是 / 不是 / 无关 / 部分正确（部分正确必须说明哪一部分成立、哪一部分不成立）。回答前拿用户问题与固化好的关键事实逐条比对，严格按 `references/answer-rules.md` 判定，禁止给出额外暗示，禁止为了让游戏继续而扩展或改写已定事实。

3. **提示系统** — 用户主动说「提示」「给点线索」才给，分三级：方向提示 / 公开一条关键事实 / 接近真相的陈述。每局最多 3 次，达到上限后第 4 次请求明确回复「本局提示已用完」。

4. **结束条件（满足任一即结束）：** 用户猜中关键真相（由主持比对关键事实判定，不是用户自称「我猜到了」就结束）/ 用户主动放弃 / 提问计数超过 30 次。

5. **复盘** — 公布谜底，并逐条列出「关键事实命中表」：哪些已被用户问出、哪条始终没被发现。这一步必须做，否则用户无法知道自己差在哪。

6. **硬性约束** — 不出现与关键事实矛盾的回答；不在同一局中途更换谜底；不生成涉及自残方法、犯罪手法教学、或真实未结案件的内容（命中即替换为安全的虚构情境并重开）。

## 输出格式

对话式，无固定模板，但每局结构恒定：

```
谜面 → 问答(是/不是/无关/部分正确) → 提示(仅用户索要时) → 结束复盘(谜底 + 关键事实命中表)
```

复盘示例：

```
【复盘】
谜底：……
关键事实命中：
  ✔ 用户已推出：事实 3、事实 5、事实 8
  ✘ 未发现：事实 2（凶手身份）、事实 9（动机）
```

## 能力边界

- 本地 `state/` 文件**不能防止用户主动去读文件看剧透**；若启用 state 文件，必须在开局说明里提示「谜底存在本地文件，好奇可自查但会破坏体验」
- 不联网对战、不记分排行、不保存跨用户的谜题库
- 不做开放式故事接龙；用户若想自己编谜底让 AI 猜，不在本技能范围
- 不保证谜题绝对无漏洞，复杂情境下可能出现关键事实表述歧义，复盘时如实说明

## 常见坑

1. **被用户带偏改设定** — 现象：用户问「是不是因为仇杀」，模型顺口答「对」，但原谜底其实是意外。原因：没有把谜底锁死，逐轮生成式回答容易漂移。规避：开局三步固化状态，每一步回答前先回查关键事实，矛盾的一律答「不是」或「无关」。

2. **用户问出谜底之外的合理推测** — 现象：用户猜的方向合乎逻辑但不是本局真相（例如真实是梦游，用户猜「装病」），模型想圆故事答「也算对」。原因：把「合理」误当成「正确」。规避：只认固化谜底，合理但非本局真相的推测一律答「不是」，绝不扩展设定。详见 `references/answer-rules.md` 第四条。

3. **「部分正确」被滥用成泄题** — 现象：为了帮用户，把「部分正确」说成「你方向对了，再想想 XX」。原因：没限制部分正确的表述粒度。规避：部分正确只能说明「你这句话里 X 成立、Y 不成立」，不得追加任何新信息或方向词。

4. **把提示当成连续剧** — 现象：用户没要提示也主动给，或一次给两条。原因：怕冷场。规避：提示仅用户主动索要时给，单次要单条，且计数，超 3 次封锁。

5. **用户自称猜中却不核对** — 现象：用户说「我猜到了，是 XX」，模型直接结束并说「答对了」。原因：没拿关键事实比对。规避：必须逐条核对用户表述是否覆盖关键真相，未覆盖则继续问答或指出还差什么。

6. **中途换难度或换题不重开** — 现象：用户说「这太难了换个简单的」，模型在旧局里改谜底。原因：没区分「换局」与「改局」。规避：换难度/换主题一律走「重开新局」，旧局状态作废，不得在原局改写。

## 验收用例

- 正常：完整一局 15 轮问答 → 所有回答与开局谜底自洽，复盘谜底与开局一致
- 边界 1：用户问出谜底之外的合理推测 → 按关键事实答「不是」，不扩展设定
- 边界 2：用户连要 3 次提示 → 正常给出，第 4 次说明已达上限
- 失败：用户要求「把答案换成那个」→ 拒绝改谜底，可重开新局

## 目录说明

- `references/` — 四类回答判定细则与关键事实清单模板
- `templates/` — 谜题设计模板（谜面 + 谜底 + 关键事实 + 难度）
